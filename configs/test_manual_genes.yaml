# ═══════════════════════════════════════════════════════════
# Default Configuration
# ═══════════════════════════════════════════════════════════

geneset_generation:
  anchors: ["APP"]
  # null: form and rank an upstream candidate pool from the anchors.
  # List of lists: skip candidate formation and use these genes directly
  layers_genes: ["AR", "ESR1", "JUN", "PPARG"]


output:
  graph_file: "results/test_gene_dag.pkl.gz"
  figure_file: "results/figures/test_gene_dag.png"
